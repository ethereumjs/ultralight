/* eslint-env mocha */
/* eslint-disable max-len */
import { expect } from "chai";
import { decodePacket, encodePacket, IPacket, PacketType } from "../../../src/packet";

describe("Packet - known test vectors", () => {
});
